STEP - 1

Authentication with default values then move to authentication with db
for db
we have to create model,principle,service,repo
then we have to define login and register method in our controller
Filters
* Security filter chain
*DAO authentication provider
*Authentication manager

// work flow
Users - diff types of users i used rbac in which customer,admin,driver,
a customer -> multiple orders
one order -> one shipment
one driver -> many shipments
many  shipment -> one driver
one vechicle-> many shipments
one shipments->many tracking

